{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "bundle": "watchify -v -t babelify js/firebase/script.js -p esmify -o bundle.js",
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2",
    "parcel-bundler": "^1.12.4",
    "prettier": "2.4.1",
    "sass": "^1.43.4"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "firebase": "^9.6.0"
  }
}
